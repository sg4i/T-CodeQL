# 贡献指南

> 如何为 CodeQL 开源项目贡献代码、查询和文档

## 贡献概览

CodeQL 是一个开源项目，欢迎社区贡献！您可以通过以下方式参与：

### 贡献类型

| 贡献类型 | 描述 | 难度 | 影响 |
|----------|------|------|------|
| **🐛 Bug 修复** | 修复现有查询的错误 | 🟢 低-中 | 🔧 维护 |
| **📝 文档改进** | 改进文档、示例、注释 | 🟢 低 | 📚 可用性 |
| **🔍 新查询** | 添加新的安全或质量查询 | 🟡 中-高 | 🚀 功能 |
| **⚡ 性能优化** | 优化现有查询性能 | 🟡 中 | 🏃 性能 |
| **🏗️ 库改进** | 改进标准库功能 | 🔴 高 | 🏛️ 基础设施 |
| **🌐 语言支持** | 添加新语言或框架支持 | 🔴 高 | 🎯 覆盖面 |

## 开始贡献

### 1. 环境准备

#### Fork 和克隆仓库

```bash
# 1. Fork GitHub 仓库
# 访问 https://github.com/github/codeql
# 点击右上角的 "Fork" 按钮

# 2. 克隆您的 fork
git clone https://github.com/YOUR_USERNAME/codeql.git
cd codeql

# 3. 添加上游仓库
git remote add upstream https://github.com/github/codeql.git

# 4. 验证远程仓库
git remote -v
# origin    https://github.com/YOUR_USERNAME/codeql.git (fetch)
# origin    https://github.com/YOUR_USERNAME/codeql.git (push)
# upstream  https://github.com/github/codeql.git (fetch)
# upstream  https://github.com/github/codeql.git (push)
```

#### 设置开发环境

```bash
# 安装 CodeQL CLI
wget https://github.com/github/codeql-cli-binaries/releases/latest/download/codeql-linux64.zip
unzip codeql-linux64.zip
export PATH=$PATH:$(pwd)/codeql

# 安装 pre-commit hooks（推荐）
pip install pre-commit
pre-commit install

# 或者使用项目提供的 hook
cp misc/scripts/pre-commit .git/hooks/pre-commit
chmod +x .git/hooks/pre-commit
```

### 2. 选择贡献方向

#### 查找适合的 Issue

```bash
# 查看 GitHub Issues
# https://github.com/github/codeql/issues

# 适合新手的标签：
# - good first issue
# - help wanted
# - documentation
# - bug

# 查看项目看板
# https://github.com/github/codeql/projects
```

#### 创建新 Issue

如果您发现了 bug 或有新想法：

```markdown
<!-- Bug Report 模板 -->
## Bug 描述
简洁地描述 bug 是什么。

## 重现步骤
1. 创建数据库：`codeql database create ...`
2. 运行查询：`codeql query run ...`
3. 观察错误结果

## 期望行为
描述您期望发生什么。

## 实际行为
描述实际发生了什么。

## 环境信息
- CodeQL 版本：
- 操作系统：
- 目标语言：

## 额外信息
添加任何其他相关信息。
```

## 贡献流程

### 1. 创建功能分支

```bash
# 同步最新代码
git checkout main
git pull upstream main

# 创建功能分支
git checkout -b feature/my-new-query
# 或
git checkout -b fix/issue-1234
# 或
git checkout -b docs/improve-readme
```

### 2. 开发和测试

#### 编写查询

```ql
/**
 * @name My New Security Query
 * @description Detects a specific security vulnerability
 * @kind path-problem
 * @problem.severity error
 * @security-severity 8.0
 * @precision high
 * @id my-org/my-new-query
 * @tags security
 *       external/cwe/cwe-XXX
 */

import python
import semmle.python.dataflow.new.TaintTracking
import DataFlow::PathGraph

// 查询实现...
```

#### 添加测试

```
test/
└── Security/
    └── MyNewQuery/
        ├── test.py              # 测试代码
        ├── MyNewQuery.qlref     # 查询引用
        └── MyNewQuery.expected  # 期望结果
```

```python
# test.py
def vulnerable_function():
    user_input = get_user_input()
    dangerous_operation(user_input)  # 应该被检测到

def safe_function():
    user_input = get_user_input()
    sanitized_input = sanitize(user_input)
    dangerous_operation(sanitized_input)  # 不应该被检测到
```

#### 运行测试

```bash
# 运行特定测试
codeql test run test/Security/MyNewQuery/

# 运行所有相关测试
codeql test run test/Security/

# 格式化查询
codeql query format queries/Security/MyNewQuery.ql --in-place

# 编译检查
codeql query compile queries/Security/MyNewQuery.ql
```

### 3. 文档编写

#### 查询帮助文档

```xml
<!-- MyNewQuery.qhelp -->
<!DOCTYPE qhelp PUBLIC "-//Semmle//qhelp//EN" "qhelp.dtd">
<qhelp>
<overview>
<p>
这个查询检测...的安全漏洞。这种漏洞可能导致...
</p>
</overview>

<recommendation>
<p>
为了修复这个问题，应该：
</p>
<ul>
<li>使用适当的输入验证</li>
<li>采用安全的 API</li>
</ul>
</recommendation>

<example>
<p>以下代码存在漏洞：</p>
<sample src="examples/bad.py" />

<p>修复后的代码：</p>
<sample src="examples/good.py" />
</example>

<references>
<li>CWE-XXX: <a href="https://cwe.mitre.org/data/definitions/XXX.html">漏洞描述</a></li>
</references>
</qhelp>
```

#### 变更日志

```markdown
<!-- change-notes/2024-01-15-my-new-query.md -->
---
category: newQuery
---
* 添加了新查询 "my-org/my-new-query" 用于检测特定的安全漏洞。
  这个查询可以发现...类型的问题，帮助开发者...
```

### 4. 提交代码

#### 提交规范

```bash
# 提交消息格式
git commit -m "类型(范围): 简短描述

详细描述（可选）

Fixes #issue-number"

# 示例
git commit -m "feat(python): add SQL injection query for Django ORM

This query detects SQL injection vulnerabilities in Django applications
that use raw SQL queries with user input.

Fixes #1234"
```

#### 提交类型

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式化
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建或辅助工具

### 5. 创建 Pull Request

#### PR 标题和描述

```markdown
<!-- PR 标题 -->
Add SQL injection query for Django ORM

<!-- PR 描述 -->
## 概述
这个 PR 添加了一个新的查询来检测 Django ORM 中的 SQL 注入漏洞。

## 变更内容
- 添加了新查询 `python/django-sql-injection`
- 包含全面的测试用例
- 添加了查询帮助文档

## 测试
- [x] 查询编译通过
- [x] 所有测试用例通过
- [x] 在真实项目中验证了查询效果

## 相关 Issue
Fixes #1234

## 检查清单
- [x] 遵循了代码风格指南
- [x] 添加了适当的测试
- [x] 更新了文档
- [x] 添加了变更日志
```

#### PR 检查清单

- [ ] **代码质量**
  - [ ] 遵循命名规范
  - [ ] 包含适当的注释
  - [ ] 没有硬编码值
  - [ ] 错误处理适当

- [ ] **测试覆盖**
  - [ ] 包含正面测试用例
  - [ ] 包含负面测试用例
  - [ ] 测试边界条件
  - [ ] 所有测试通过

- [ ] **文档完整**
  - [ ] 查询元数据完整
  - [ ] 包含 .qhelp 文件
  - [ ] 代码注释清晰
  - [ ] 更新了相关文档

- [ ] **性能考虑**
  - [ ] 查询性能合理
  - [ ] 使用了适当的优化
  - [ ] 没有明显的性能问题

## 查询开发指南

### 1. 查询分类和放置

#### 目录结构

```
<language>/ql/src/
├── Security/              # 安全相关查询
│   ├── CWE-XXX/          # 按 CWE 分类
│   └── experimental/      # 实验性查询
├── Quality/               # 代码质量查询
├── Performance/           # 性能相关查询
└── Metrics/              # 代码指标查询
```

#### 查询 ID 规范

```
格式: <language>/<category>-<description>

示例:
- py/sql-injection
- java/spring-sql-injection
- js/prototype-pollution
- go/goroutine-leak
```

### 2. 实验性查询

新查询通常先放在 `experimental/` 目录中：

```
python/ql/src/experimental/Security/
└── MyNewQuery/
    ├── MyNewQuery.ql
    ├── MyNewQuery.qhelp
    └── examples/
        ├── bad.py
        └── good.py
```

#### 实验性查询要求

- 必须包含 `experimental` 标签
- 可能有更多假阳性
- 需要社区反馈和验证
- 经过验证后可移至正式目录

### 3. 查询质量标准

#### 精确度要求

| 精确度 | 假阳性率 | 要求 |
|--------|----------|------|
| `very-high` | < 5% | 生产环境可用 |
| `high` | < 15% | 默认启用 |
| `medium` | < 30% | 需要配置启用 |
| `low` | > 30% | 仅用于研究 |

#### 性能要求

```bash
# 查询应在合理时间内完成
# 小型项目 (< 10K LOC): < 30秒
# 中型项目 (10K-100K LOC): < 5分钟
# 大型项目 (> 100K LOC): < 30分钟

# 测试查询性能
time codeql query run MyQuery.ql --database=test-db
```

## 代码审查流程

### 1. 自我审查

提交 PR 前的自我检查：

```bash
# 运行完整的测试套件
codeql test run test/

# 检查代码格式
find . -name "*.ql" -exec codeql query format {} --check-only \;

# 编译所有查询
find . -name "*.ql" -exec codeql query compile {} \;

# 运行 linter（如果有）
./scripts/run-linter.sh
```

### 2. 同行审查

#### 审查要点

**功能性审查：**
- 查询逻辑是否正确？
- 是否覆盖了所有相关场景？
- 是否有遗漏的边界情况？

**性能审查：**
- 查询是否高效？
- 是否使用了适当的优化？
- 是否会导致性能问题？

**可维护性审查：**
- 代码是否易于理解？
- 命名是否清晰？
- 注释是否充分？

#### 审查反馈示例

```markdown
## 审查反馈

### 👍 做得好的地方
- 查询逻辑清晰，易于理解
- 测试用例覆盖全面
- 文档写得很好

### 🔧 需要改进的地方
1. **性能优化**: 第23行的条件可以提前，减少计算量
   ```ql
   // 建议改为
   where 
     f.getName() = "specific_name" and  // 先用强限制
     f.getAParameter().getType() instanceof RefType
   ```

2. **边界情况**: 缺少对空字符串的处理
   - 建议添加测试用例：`test_empty_string()`

3. **命名规范**: 变量 `x` 应该使用更描述性的名称

### ❓ 问题
- 这个查询是否应该检测间接调用？
- 对于 Python 2 和 3 的兼容性如何？

### 📝 小建议
- 考虑添加一个使用示例到 README
- 查询帮助文档可以更详细一些
```

### 3. 维护者审查

维护者会进行最终审查：

- **架构一致性**: 是否符合项目架构
- **API 兼容性**: 是否破坏现有 API
- **文档完整性**: 是否需要更新其他文档
- **发布影响**: 对用户的影响如何

## 社区参与

### 1. 讨论和反馈

#### GitHub Discussions

参与社区讨论：
- **Ideas**: 分享新想法
- **Q&A**: 提问和回答问题
- **Show and tell**: 展示您的工作
- **General**: 一般性讨论

#### 代码审查参与

即使不是维护者，也可以参与代码审查：
- 测试 PR 中的查询
- 提供反馈和建议
- 分享使用经验

### 2. 知识分享

#### 博客文章

分享您的 CodeQL 经验：
- 查询开发技巧
- 真实案例分析
- 性能优化经验
- 框架集成方法

#### 会议演讲

在技术会议上分享：
- 安全会议（Black Hat, DEF CON）
- 开发者会议（GitHub Universe）
- 学术会议（Security 相关）

### 3. 教育和培训

#### 创建教程

- 编写入门教程
- 制作视频教程
- 设计练习题目
- 开发 CTF 挑战

#### 指导新手

- 回答新手问题
- 审查新手的 PR
- 提供学习建议
- 分享最佳实践

## 长期贡献路径

### 1. 贡献者等级

#### 新手贡献者
- 修复文档错误
- 添加测试用例
- 修复简单 bug
- 改进错误消息

#### 经验贡献者
- 开发新查询
- 优化现有查询
- 改进库功能
- 参与代码审查

#### 核心贡献者
- 设计新功能
- 架构决策参与
- 指导其他贡献者
- 维护项目质量

#### 维护者
- 管理发布流程
- 制定项目方向
- 处理复杂问题
- 社区管理

### 2. 专业发展

#### 技能提升

通过贡献 CodeQL 可以提升：
- **静态分析技能**: 深入理解代码分析
- **安全知识**: 学习各种安全漏洞
- **编程语言**: 掌握多种编程语言
- **开源协作**: 学习开源项目管理

#### 职业机会

CodeQL 贡献可以带来：
- **求职优势**: 展示技术能力
- **行业认知**: 在安全社区建立声誉
- **网络建设**: 结识同行专家
- **学习机会**: 接触前沿技术

### 3. 认可和奖励

#### 官方认可

- **贡献者列表**: 在项目中列出贡献者
- **发布说明**: 在发布说明中感谢贡献者
- **博客文章**: GitHub 博客可能会介绍重要贡献

#### 社区认可

- **GitHub 成就**: 获得相关徽章
- **社交媒体**: 在 Twitter 等平台被提及
- **会议邀请**: 被邀请在会议上演讲

## 常见问题

### Q: 我是新手，应该从哪里开始？

**A**: 建议从以下开始：
1. 阅读现有查询，理解结构
2. 修复文档中的小错误
3. 为现有查询添加测试用例
4. 尝试编写简单的查询

### Q: 如何确保我的查询是有用的？

**A**: 
1. 在真实项目中测试查询
2. 确保假阳性率较低
3. 与社区讨论查询的价值
4. 参考 OWASP、CWE 等标准

### Q: 我的 PR 被拒绝了，怎么办？

**A**:
1. 仔细阅读审查反馈
2. 根据建议修改代码
3. 如有疑问，在 PR 中讨论
4. 不要气馁，这是学习过程

### Q: 如何提高查询的性能？

**A**:
1. 使用 `cached` 标注昂贵的谓词
2. 提前使用限制性强的条件
3. 避免笛卡尔积
4. 使用 `exists` 限制作用域
5. 参考性能优化指南

### Q: 我可以贡献非英语的文档吗？

**A**: 
目前项目主要使用英语，但欢迎：
1. 翻译重要文档
2. 创建本地化的教程
3. 在社区中用母语帮助他人

## 联系方式

### 获取帮助

- **GitHub Issues**: 报告 bug 或请求功能
- **GitHub Discussions**: 一般性讨论和问题
- **Stack Overflow**: 使用 `codeql` 标签提问
- **Twitter**: 关注 @GitHubSecurity

### 紧急联系

对于安全相关的紧急问题：
- **安全邮箱**: security@github.com
- **漏洞报告**: https://bounty.github.com

---

**感谢您对 CodeQL 的贡献！** 🙏 

每一个贡献，无论大小，都让 CodeQL 变得更好，让整个软件开发社区更加安全。让我们一起构建更安全的软件世界！ 🌍🔒
